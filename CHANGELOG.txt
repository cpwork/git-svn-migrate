Version 2.0
-----------
- Migrate from inline structure to functions
- Use BASH's getopts instead of manuially parsing parameters

Version 1.x
-----------
- Add option to make shared git repositories
- Don't use --no-metadata by default, but show it as an usage option
- Add ability to pass options to git-svn directly
- Allow git-svn-migrate to be called from any directory
- Make dependency on bash explicit

Version 1.0
-----------
- Initial release
